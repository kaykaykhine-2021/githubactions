name: main-name
on: [push]
jobs:
  job-name:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
          echo "(1) ths is env=$ENV_1"
        env: 
          ENV_1: hashito
      - run: |
          echo "(2) ths is env=$ENV_1"
